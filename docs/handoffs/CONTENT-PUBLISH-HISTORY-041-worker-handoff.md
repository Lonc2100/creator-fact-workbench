# CONTENT-PUBLISH-HISTORY-041 Worker Handoff

## Task ID

CONTENT-PUBLISH-HISTORY-041

## Completed Work

- Added compact per-content publish history to `/content` content detail.
  - The detail now shows the selected content's platform versions, queue context, and local publish records.
  - Each platform version row shows status, `scheduledAt`, `publishedAt`, `confirmationSource`, and failure/note context.
  - Rows include a local "select version" action and a link to `/calendar?versionId=...#publish-ledger`.
- Extended the dedicated content workbench snapshot with read-only `publishRecords`.
  - This keeps `/content` on `/api/self-media/content-workbench`.
  - It does not reuse trusted dashboard contents as the source for the workbench.
- Added calendar URL support for `?versionId=...`.
  - This lets links from `/content` open `/calendar` with the corresponding version selected and the ledger anchor available.
- Kept publish history read-only.
  - No new publish API was added.
  - Existing manual confirmation semantics remain owned by the existing `content-versions` confirm path.
- Preserved trusted metric boundaries.
  - Publish records remain local/manual operating records.
  - Publish records are not trusted metric evidence and do not change dashboard/review totals.
- Strengthened tests and browser smoke.
  - Service contract now verifies content workbench carries publish records while trusted totals stay unchanged.
  - UI harness now locks the content publish history copy and hidden boundary.
  - Draft-review browser E2E now returns to `/content`, checks publish history visibility, validates the calendar ledger link, and saves a content-history screenshot.

## Changed Files

- `src/domain/self-media/types/self-media-types.ts`
- `src/domain/self-media/service/self-media-service.ts`
- `src/domain/self-media/ui/patterns/ContentManagement.tsx`
- `src/domain/self-media/ui/screens/ContentPage.tsx`
- `src/domain/self-media/ui/screens/CalendarPage.tsx`
- `src/app/globals.css`
- `tests/self-media-contract.test.ts`
- `tests/ui-harness.test.mjs`
- `scripts/draft-review-ui-e2e-039.mjs`
- `docs/handoffs/CONTENT-PUBLISH-HISTORY-041-worker-handoff.md`

Note: the repository already has many unrelated modified/untracked files from earlier tasks. I did not revert, delete, or clean them.

## Verification

- `npm run smoke:draft-review-ui-e2e` PASS
  - isolated DB: yes
  - isolated `NEXT_DIST_DIR`: yes
  - `ledgerVisible: true`
  - `contentPublishHistoryVisible: true`
  - `contentPublishHistoryChecked: true`
  - trusted totals before/after unchanged: 4 trusted contents, 4 trusted metric snapshots, 6750 trusted views.
- `npm run test:self-media` PASS
  - 121 tests passed.
- `npm run typecheck` PASS
- `npm run verify:harness` PASS
  - includes typecheck, context check, arch lint, structure tests, reference tests, UI harness, self-media tests, entropy tests, agent trajectory tests, and template doctor.
- `git diff --check` PASS
  - Only warning observed: `tsconfig.json` CRLF will be replaced by LF the next time Git touches it. No whitespace errors.

## Screenshots

- Content publish history screenshot: `.local/draft-review-ui-e2e-039/content-publish-history.png`
- Calendar ledger screenshot: `.local/draft-review-ui-e2e-039/calendar-published.png`

I visually inspected the content publish history screenshot. It shows the `/content` detail with the read-only publish history table, `manual` confirmation source, local note, publish record badge, and calendar ledger jump.

## Known Issues

- The working tree remains dirty with many pre-existing unrelated changes and untracked historical handoffs/scripts.
- `git diff --check` still prints the existing CRLF warning for `tsconfig.json`.
- The screenshot is generated by the isolated draft-review E2E smoke under `.local/`; it is not a persistent product asset.

## Boundaries Kept

- Did not add a new publish API.
- Did not call real platform publish APIs.
- Did not make publish history editable.
- Did not make publish records trusted metric evidence.
- Did not change dashboard/reviews trusted totals or default trusted scope.
- Did not delete files, clean ports, or kill processes.

## Next Recommendation

- Orchestrator should review the `/content` detail layout and decide whether the compact table is sufficient or should later move to a collapsible subpanel when per-content publish records grow.
- No follow-up is required to preserve current trusted-data semantics.

## Orchestrator Decision Required

Yes.
