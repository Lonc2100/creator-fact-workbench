# CONTENT-CALENDAR-DATA-ONLY-042 Worker Handoff

## Task ID

CONTENT-CALENDAR-DATA-ONLY-042

## Completed Work

- Cleaned `/content` default view so the first screen prioritizes operating-useful rows:
  - content already entering the operating dashboard;
  - review-needed or scheduled platform drafts;
  - drafts generated from action items.
- Kept all-local/history rows available only through explicit diagnostic filters:
  - `全部本地/诊断`;
  - manual imports;
  - external imports;
  - unclassified diagnostic rows.
- Removed visible long content/version/action identifiers from default `/content` tables and details.
- Replaced engineering labels in `/content` with business-facing language:
  - no visible `local_workflow`;
  - no visible `sourceKinds`;
  - no visible `trusted/local` count wording;
  - `platform versions` became `平台稿件`;
  - `publish history/ledger` became `发布历史/台账`.
- Cleaned `/calendar` default view so it only shows real actionable scheduling and manual publish ledger information:
  - default calendar uses only dashboard-visible operating content or workflow drafts;
  - default calendar excludes paused WeChat and draft rows without a real scheduled/published time;
  - diagnostic/all-local records require explicit `全部本地/诊断` scope.
- Removed fake-looking disabled calendar controls from the default toolbar:
  - no disabled `List`;
  - no disabled `Autolists`;
  - no disabled `Best times`;
  - no disabled fake new-post button.
- Added an empty state to the calendar canvas when no actionable schedule exists instead of showing fake `+` slot cards.
- Kept publish ledger clearly local/manual:
  - no platform publish API call;
  - no metric evidence claim;
  - local manual publish records remain separate from operating metric totals.

## Changed Files

- `src/domain/self-media/ui/screens/ContentPage.tsx`
  - Added default `operating_default` source scope.
  - Added operating-priority filtering and sorting.
  - Updated header badges and summary copy to show operating counts first.
  - Hid long content ids in trusted-scope curation.
- `src/domain/self-media/ui/patterns/ContentManagement.tsx`
  - Removed default visible source/id/debug wording.
  - Reworded content table, details, publish history, and queue references into operator-facing copy.
  - Kept publish confirmation copy explicit and manual.
- `src/domain/self-media/ui/screens/CalendarPage.tsx`
  - Added calendar scope filtering for `默认运营排期` vs `全部本地/诊断`.
  - Added publish ledger scope filtering for `默认运营台账` vs `全部记录/诊断`.
  - Excluded paused WeChat and non-actionable rows from default calendar/ledger.
  - Removed disabled decorative controls.
- `src/domain/self-media/ui/patterns/PublishCalendar.tsx`
  - Added `showEmptySlots` control.
  - Added default empty state for no actionable schedule.
- `tests/ui-harness.test.mjs`
  - Updated tests for data-only default content/calendar views.
  - Added assertions that default content/calendar source does not expose internal labels like `local_workflow`, `sourceKinds`, `trusted/local`, `Publish ledger`, `Best times`, `Autolists`, smoke/fixture/demo wording.
  - Updated stale dashboard/import/reviews UI harness expectations to the current Operator View Data Only wording while leaving business code untouched.

## Screenshots

- `.local/content-calendar-data-only-042-content.png`
- `.local/content-calendar-data-only-042-calendar.png`

Screenshot text scan:

- `/content` default scan PASS: no `local_workflow`, `sourceKinds`, `Platform Versions`, `Content Workbench`, or `trusted /` wording found.
- `/calendar` default scan PASS: no `Best times`, `Autolists`, `Publish ledger`, `fixture`, `smoke`, `demo/fake`, or `local_workflow` wording found.

## Verification

- `npm run test:self-media` PASS
  - 121/121 tests passing.
- `npm run typecheck` PASS
- `npm run verify:harness` PASS
  - includes typecheck, context check, arch lint, structure/reference/UI harness/self-media/entropy/agent trajectory/template doctor.
- `git diff --check` PASS
  - exited 0.
  - warning only: existing `tsconfig.json` CRLF normalization note.
- Screenshot capture PASS
  - Existing `http://127.0.0.1:3200/content` and `/calendar` returned 200.
  - No dev server was started for screenshot capture.

## Safety Notes

- No data logic for trusted dashboard/reviews totals was changed.
- No collector, importer, or platform API was called.
- No real platform publish API was added or called.
- No publish record is generated by scheduling.
- No DB was deleted, migrated, or cleaned.
- Publish ledger remains local manual confirmation only and does not become trusted metric evidence.
- Paused WeChat remains excluded from default calendar operation.

## Known Issues

- `/content` still includes a visible explicit `全部本地/诊断` filter because operators need a way to inspect historical/local rows without changing trusted totals. It is not the default scope.
- `/calendar` default still uses the existing time-grid visual. Empty default cells no longer show fake `+` slot cards, but the grid itself remains as the scheduling canvas.
- The working tree contains many pre-existing dirty/untracked files from prior tasks; this handoff lists only files touched for 042.

## Next Recommendation

- Have the orchestrator review whether the trusted-scope curation panel should remain above the content workbench table or move behind a secondary "看板口径" tab in the next UI cleanup pass.

## Orchestrator Decision Required

Yes.
